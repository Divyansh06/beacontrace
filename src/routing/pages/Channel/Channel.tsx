import ChannelEvents from "../../../components/ChannelEvents/ChannelEvents";
import ChannelInfo from "../../../components/ChannelInfo/ChannelInfo";

function Channel() {
    return (
        <>
            <ChannelInfo />
            <ChannelEvents />
        </>

    );
}

export default Channel;